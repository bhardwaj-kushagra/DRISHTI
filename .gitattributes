# Ensure consistent text file line endings
* text=auto

# Treat common binary types
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.gz binary
*.7z binary

# Lock files - keep as-is
package-lock.json -text
pnpm-lock.yaml -text
yarn.lock -text

# Normalize shell scripts with LF (even on Windows)
*.sh text eol=lf
